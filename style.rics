
/*
 *
 * Max Performance - Opacity based karaoke
 * v1.10.4
 * 2026-09-14
 *
 * Minimal theme can be turned off easily.
 *
 * Please be careful removing something.
 *
 * _"Aye! I'm not saying I lie, but lying sure is fun!" — Boidu (hypothetically)_
 *
 */

$enable-minimal: on;
/* on / off */

$karaoke-style: reveal;
/* reveal / flash / trail / spotlight */

$enable-blur: off;
/* on / off */

$blur-amount: 30px;

 /* ++++++++++++++++++++++++++++++++++++++++++++++++++
   A. MINIMAL THEME TOGGLE
   ++++++++++++++++++++++++++++++++++++++++++++++++++
 *
 *   on  : applies minimal theme
 *   off : no minimal theme
 * 
 * How to use it?
 *   Just write on or off.
 *
 *
 * ++++++++++++++++++++++++++++++++++++++++++++++++++
   B. KARAOKE STYLE PRESETS  (richsync only)
   ++++++++++++++++++++++++++++++++++++++++++++++++++
 *
 *   reveal    : words float and appear one by one, then
 *                 the whole line disappears when it's finished. (default)
 *   flash     : each word flashes only once and then disappears.
 *   trail     : words appear and disappear like a
 *                 soft trailing streak of light.
 *   spotlight : the whole line is visible at once, 
 *                 with the current word standing out.
 * 
 * How to use it?
 *   Just write the name of the style you want to use in
 *     - $karaoke-style: flash; - for example writing flash
 *     would activate the flash style
 *
 * ++++++++++++++++++++++++++++++++++++++++++++++++++
   C. BACKGROUND BLUR TOGGLE
   ++++++++++++++++++++++++++++++++++++++++++++++++++
 *
 *   on  : blur effect is enabled (blur-amount changes how much).
 *   off : blur is off. Meaningful
 *           Keeping blur off is better for low-end devices.
 *
 * ------------------------------------------------------- */



$karaoke-style-presets: (
  reveal: (
    past-word-opacity: 0.7,
    future-word-opacity: 0,
    word-extra-duration: 200ms,
    fade-start: 75,
    rise-duration: 300ms
  ),
  flash: (
    past-word-opacity: 0,
    future-word-opacity: 0,
    word-extra-duration: 100ms,
    fade-start: 85,
    rise-duration: 0ms
  ),
  trail: (
    past-word-opacity: 0,        
    future-word-opacity: 0,
    word-extra-duration: 700ms,  
    fade-start: 60,
    rise-duration: 700ms
  ),
  spotlight: (
    past-word-opacity: 0.6,
    future-word-opacity: 0.45,
    word-extra-duration: 200ms,
    fade-start: 75,
    rise-duration: 220ms
  )
);

$effect-map: map-get($karaoke-style-presets, $karaoke-style);

$word-rise-keyframe: word-rise;
@if $karaoke-style == trail {
  $word-rise-keyframe: word-rise-trail;
}
@if $karaoke-style == flash {
  $word-rise-keyframe: word-rise-flash;
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   GLOBAL SETUP
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

@import url("https://fonts.googleapis.com/css2?family=Roboto+Flex:opsz,wght@8..144,100..1000&display=swap");
@import url('https://fonts.googleapis.com/css2?family=Google+Sans+Flex:opsz,wght@6..144,1..1000&display=swap');

* {
  --whole-website-font-family: "Google Sans Flex", "Roboto Flex", Roboto, var(--noto-sans-universal), sans-serif, system-ui;
  font-family: var(--whole-website-font-family) !important;
  font-optical-sizing: auto;
  scrollbar-width: thin;
}

/*
;
blyrics-target-scroll-pos-ratio = 0.4;
;
*/

:root {
  /* hover and scroll opacities */
  --opacity-non-hovered: .7;
  --opacity-hovered: .95;
  --opacity-scrolling: var(--opacity-non-hovered, 0.85);
  
  /* translation & romanization opacity multiplier */
  --opacity-trans-ratio: .82;
  --opacity-roman-ratio: .55;

  /* karaoke or "richsync" opacities */
  --opacity-words-past-line: 0;
  --opacity-words-future-line: 0;
  --opacity-past-word-current-line: #{map-get($effect-map, past-word-opacity)};
  --opacity-current-word-current-line: 1;
  --opacity-future-word-current-line: #{map-get($effect-map, future-word-opacity)};

  --word-rise-duration: #{map-get($effect-map, rise-duration)};

  /* line sync or "synced" opacities */
  --opacity-line-past: 0;
  --opacity-line-future: 0;
  --opacity-line-current: 0.87;

  /* without sync or "unsynced" opacities */
  --opacity-unsynced: .87;

  /* other variables */
  --blyrics-scale: 1;
  --blyrics-active-scale: 1;
  --blyrics-lyric-scroll-duration: 750ms;
  --blyrics-lyric-scroll-timing-function: ease-in-out;
  --blyrics-font-weight: 700;
  --transition-curve: ease-in-out;
  --blyrics-loader-transition-easing: cubic-bezier(0.34, 1.3, 0.64, 1);

  /* controls early/lateness of scrolling lines */
  --scroll-timing-offset-richsync: 0.65s; /* karaoke */
  --scroll-timing-offset-synced: 0.5s;    /* line sync */

  /* controls earliness/lateness of words being sung for karaoke */
  --highlight-timing-offset-richsync: -280ms;

  /* controls earliness/lateness of lines being sung for line-sync */
  --highlight-timing-offset-synced: 0s;

  /* duration of entry/exit animation of lines */
  --richsync-entry-duration: 150ms;
  --richsync-exit-duration: 200ms;
  --synced-entry-duration: 250ms;
  --synced-exit-duration: 250ms;

}

/* in vain effort to improve efficiency on low-end devices */
#blyrics-loader,
#blyrics-wrapper,
#blyrics-loader:not([state="showing-message"]):not([state="exiting-message"]):after,
#blyrics-ad-overlay:after,
.autoscroll-resume-button,
.blyrics-container,
.blyrics--line,
.blyrics--word.blyrics--animating,
.blyrics--word::after,
.blyrics--word.blyrics--animating::after {
  will-change: unset;
}

@property --blyrics-richsync-line-base-opacity {
  syntax: "<number>";
  inherits: false;
  initial-value: 1;
}

@property --blyrics-synced-line-base-opacity {
  syntax: "<number>";
  inherits: false;
  initial-value: 1;
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SCROLL TRIGGER TIMING (richsync & synced only)
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

#blyrics-wrapper .blyrics-container[data-sync="richsync"] {
  --blyrics-scroll-timing-offset: var(--scroll-timing-offset-richsync, 0.5s);
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] {
  --blyrics-scroll-timing-offset: var(--scroll-timing-offset-synced, 0.5s);
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 1 — MAIN LINE TEXT
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

/* ─ A ─ [data-sync="richsync"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--word::after {
  display: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line {
  opacity: 1;
  transition-property: transform;
  transition-duration: .6s;
  transition-timing-function: ease-in-out;

  & > span {
    opacity: var(--blyrics-richsync-line-base-opacity);
    transition: none;
  }
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line > span,
#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line > .blyrics--translated,
#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line > .blyrics--romanized {
  --blyrics-richsync-line-base-opacity: var(--opacity-words-future-line);
  --blyrics-synced-line-base-opacity: var(--opacity-words-future-line);
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--word {
  opacity: 1;
  transition: opacity 0.2s ease;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line.blyrics--animating > span:not(.blyrics--break) {
  will-change: opacity;
  transition: none;
}

$richsync-opacity-targets: (
  main:       (sel: "> span:not(.blyrics--break)",              ratio: 1),
  translated: (sel: "> .blyrics--translated", ratio: var(--opacity-trans-ratio)),
  romanized:  (sel: "> .blyrics--romanized",  ratio: var(--opacity-roman-ratio))
);

@mixin richsync-opacity-anim($name, $target) {
  @keyframes richsync-#{$name}-entry {
    0% {
      opacity: calc(var(--opacity-words-future-line) / var(--opacity-future-word-current-line) * #{map-get($target, ratio)});
      animation-timing-function: ease;
    }
    100% {
      opacity: calc(var(--opacity-current-word-current-line) * #{map-get($target, ratio)});
    }
  }
  @keyframes richsync-#{$name}-exit {
    0% {
      opacity: calc(var(--opacity-current-word-current-line) * #{map-get($target, ratio)});
      animation-timing-function: ease-in;
    }
    100% {
      opacity: calc(var(--opacity-words-past-line) / var(--opacity-past-word-current-line) * #{map-get($target, ratio)});
    }
  }
}

@each $name, $target in $richsync-opacity-targets {
  @include richsync-opacity-anim($name, $target);

  #blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line.blyrics--animating #{map-get($target, sel)} {
    animation-name: richsync-#{$name}-entry, richsync-#{$name}-exit;
    animation-duration: var(--richsync-entry-duration), var(--richsync-exit-duration);
    animation-delay:
      calc(var(--blyrics-anim-delay) - var(--richsync-entry-duration) + var(--highlight-timing-offset-richsync, 0s)),
      max(
        calc(var(--blyrics-anim-delay) + var(--highlight-timing-offset-richsync, 0s)),
        calc(var(--blyrics-anim-delay) + var(--blyrics-duration) - var(--richsync-exit-duration) + var(--highlight-timing-offset-richsync, 0s))
      );
    animation-timing-function: linear;
    animation-fill-mode: both, forwards;
    will-change: opacity;
  }
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--word.blyrics--animating {
  will-change: unset;
  transition: opacity 0.2s ease;
  animation-name: #{$word-rise-keyframe}, word-sing;
  animation-duration: calc(var(--blyrics-duration) + var(--word-rise-duration) ), calc(var(--blyrics-duration) + #{map-get($effect-map, word-extra-duration)});
  animation-fill-mode: both;
  animation-timing-function: ease-out;
  animation-delay: calc(var(--blyrics-anim-delay) + var(--highlight-timing-offset-richsync, 0s));
  will-change: opacity;
}

#blyrics-wrapper .blyrics-container .blyrics--word.blyrics--paused,
#blyrics-wrapper .blyrics-container .blyrics--line.blyrics--paused .blyrics--word,
#blyrics-wrapper .blyrics-container .blyrics--line.blyrics--paused,
#blyrics-wrapper .blyrics-container .blyrics--line.blyrics--paused > span,
#blyrics-wrapper .blyrics-container .blyrics--line.blyrics--paused :is(.blyrics--translated, .blyrics--romanized)
{
  animation-play-state: paused;
}

@keyframes word-sing {
  0% {
    opacity: var(--opacity-future-word-current-line);
    animation-timing-function: ease;
  }
  10% {
    opacity: var(--opacity-current-word-current-line);
  }
  #{map-get($effect-map, fade-start)}% {
    opacity: var(--opacity-current-word-current-line);
    animation-timing-function: ease-in-out;
  }
  100% {
    opacity: var(--opacity-past-word-current-line);
  }
}

@keyframes word-rise {
  0% {
    transform: translate(0) scale(1);
    animation-timing-function: ease;
  }
  30% {
    transform: translateY(-0.15em) translateX(0.15em) scale(1.035);
    animation-timing-function: ease-in;
  }
  100% {
    transform: translate(0) scale(1);
  }
}

/* trail-only variant */
@keyframes word-rise-trail {
  0% {
    translate: 0.25em 0;
    transform: scaleX(0.85) scaleY(0.9) skewX(-4deg);
    animation-timing-function: cubic-bezier(0.16, 1, 0.3, 1);
    transform-origin: right center;
  }
  10% {
    transform: scaleX(0.96) scaleY(0.96) skewX(-2deg);
    animation-timing-function: ease-out;
    transform-origin: right center;
  }
  30% {
    transform: scale(1) skewX(0deg);
    transform-origin: right center;
  }
  60% {
    translate: 0 0;
    transform: scale(1) skewX(0deg);
    animation-timing-function: cubic-bezier(0.7, 0, 0.84, 0);
    transform-origin: right center;
  }
  100% {
    translate: -0.25em 0;
    transform: scaleX(1.35) scaleY(0.9) skewX(-10deg);
    transform-origin: right center;
  }
}

@keyframes word-rise-flash {
  0% {
    transform: translate(0) scale(1);
    animation-timing-function: ease;
    transform-origin: bottom;
  }
  30% {
    transform: translateX(0em) translateY(-0.15em) scale(1.035);
    animation-timing-function: ease-in-out;
    transform-origin: bottom;
  }
  100% {
    transform: translate(0) scale(1);
    transform-origin: bottom;
  }
}


/* ─ B ─ [data-sync="synced"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--word::after {
  display: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] {
  --blyrics-lyric-scroll-duration: 1.5s;
  --blyrics-lyric-scroll-timing-function: cubic-bezier(0.27, 1.06, 0.18, 1.00);
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line {
  opacity: var(--blyrics-synced-line-base-opacity);
  transition-property: transform;
  transition-duration: .25s;
  transition-timing-function: ease-in-out;
  transition-delay: 0s;
  --blyrics-synced-line-base-opacity: var(--opacity-line-future);
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line.blyrics--animating {
  animation-name: line-entry, line-exit;
  animation-duration: var(--synced-entry-duration), var(--synced-exit-duration);
  animation-delay:
    calc(var(--blyrics-anim-delay) - var(--synced-entry-duration) + var(--highlight-timing-offset-synced, 0s)),
    max(
      calc(var(--blyrics-anim-delay) + var(--highlight-timing-offset-synced, 0s)),
      calc(var(--blyrics-anim-delay) + var(--blyrics-duration) - var(--synced-exit-duration) + var(--highlight-timing-offset-synced, 0s))
    );
  animation-timing-function: ease-in-out;
  animation-fill-mode: both, forwards;
  will-change: opacity;
}

@keyframes line-entry {
  0%   { opacity: var(--opacity-line-future); animation-timing-function: ease; }
  100% { opacity: var(--opacity-line-current); }
}

@keyframes line-exit {
  0%   { opacity: var(--opacity-line-current); animation-timing-function: ease-out; }
  100% { opacity: var(--opacity-line-past); }
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--word {
  opacity: 1;
  transition: none;
  animation: none !important;
}

/* ─ C ─ [data-sync="none"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--word::after {
  display: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line {
  opacity: var(--opacity-unsynced);
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line > span {
  color: var(--blyrics-lyric-active-color) !important;
  opacity: 1;
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 2 — TRANSLATED TEXT
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

/* ─ A ─ [data-sync="richsync"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--translated > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--translated {
  margin-top: .4em;
  font-weight: 600;
  line-height: 1.5;
  font-size: .7071em;
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--translated:has(.blyrics--word) {
  opacity: calc(var(--blyrics-richsync-line-base-opacity) * var(--opacity-trans-ratio));
  transition: opacity 0.2s ease;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--translated:not(:has(.blyrics--word)) {
  opacity: calc(var(--blyrics-synced-line-base-opacity) * var(--opacity-trans-ratio));
  transition: opacity 0.2s ease;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line.blyrics--animating .blyrics--translated {
  transition: opacity 0.2s ease;
  will-change: opacity;
}

/* ─ B ─ [data-sync="synced"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--translated > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--translated {
  margin-top: .4em;
  font-weight: 600;
  line-height: 1.5;
  font-size: .7071em;
  color: var(--blyrics-lyric-active-color) !important;
  opacity: var(--opacity-trans-ratio);
  transition: none;
}

/* ─ C ─ [data-sync="none"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--translated > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--translated {
  margin-top: .4em;
  font-weight: 600;
  line-height: 1.5;
  font-size: .7071em;
  color: var(--blyrics-lyric-active-color) !important;
  opacity: var(--opacity-trans-ratio);
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 3 — ROMANIZED TEXT
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

/* ─ A ─ [data-sync="richsync"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--romanized {
  width: auto;
  padding-block: initial;
  background: 0;
  padding: initial;
  border: 0;
  border-radius: 0;
  margin-top: .2em;
  font-size: .6429em;
  margin-bottom: .2em;
  font-weight: 350;
  letter-spacing: .03em;
  line-height: 1.5;
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--romanized > span {
  color: var(--blyrics-lyric-active-color) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--romanized:has(.blyrics--word) {
  opacity: calc(var(--blyrics-richsync-line-base-opacity) * var(--opacity-roman-ratio));
  transition: opacity 0.2s ease;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--romanized:not(:has(.blyrics--word)) {
  opacity: calc(var(--blyrics-synced-line-base-opacity) * var(--opacity-roman-ratio));
  transition: opacity 0.2s ease;
}
#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line.blyrics--animating .blyrics--romanized {
  will-change: opacity;
  transition: opacity 0.2s ease;
}

/* ─ B ─ [data-sync="synced"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--romanized {
  width: auto;
  padding-block: initial;
  background: 0;
  padding: initial;
  border: 0;
  border-radius: 0;
  margin-top: .2em;
  font-size: .6429em;
  margin-bottom: .2em;
  font-weight: 350;
  letter-spacing: .03em;
  line-height: 1.5;
  color: var(--blyrics-lyric-active-color) !important;
  opacity: var(--opacity-roman-ratio);
  transition: none;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--romanized > span {
  color: var(--blyrics-lyric-active-color) !important;
}

/* ─ C ─ [data-sync="none"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--romanized {
  width: auto;
  padding-block: initial;
  background: 0;
  padding: initial;
  border: 0;
  border-radius: 0;
  margin-top: .2em;
  font-size: .6429em;
  margin-bottom: .2em;
  font-weight: 350;
  letter-spacing: .03em;
  line-height: 1.5;
  color: var(--blyrics-lyric-active-color) !important;
  opacity: var(--opacity-roman-ratio);
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--romanized > span {
  color: var(--blyrics-lyric-active-color) !important;
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 4 — HOVER
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

/* ─ A ─ [data-sync="richsync"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="richsync"]:hover .blyrics--line:not(:hover) {
  transform: scale(1) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line.blyrics--line.blyrics--line:hover {
  transform: scale(1.1) !important;
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line:hover .blyrics--word {
  opacity: var(--opacity-hovered) !important;
  transition: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"]:hover {
  --opacity-words-past-line: var(--opacity-scrolling);
  --opacity-words-future-line: var(--opacity-scrolling);
  --opacity-past-word-current-line: var(--opacity-scrolling);
  --opacity-future-word-current-line: var(--opacity-scrolling);
  --opacity-current-word-current-line: 1;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"]:hover .blyrics--line {
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line:hover .blyrics--translated {
  opacity: calc(var(--opacity-hovered) * var(--opacity-trans-ratio));
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"] .blyrics--line:hover .blyrics--romanized {
  opacity: calc(var(--opacity-hovered) * var(--opacity-roman-ratio));
}

/* ─ B ─ [data-sync="synced"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="synced"]:hover .blyrics--line:not(:hover) {
  transform: scale(1) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line.blyrics--line.blyrics--line:hover {
  transform: scale(1.1) !important;
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line:hover .blyrics--word {
  opacity: var(--opacity-hovered) !important;
  transition: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"]:hover {
  --opacity-line-past: var(--opacity-scrolling);
  --opacity-line-future: var(--opacity-scrolling);
}

#blyrics-wrapper .blyrics-container[data-sync="synced"]:hover .blyrics--line {
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line:hover .blyrics--translated {
  transition: none !important;
  opacity: calc(var(--opacity-hovered) * var(--opacity-trans-ratio)) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="synced"] .blyrics--line:hover .blyrics--romanized {
  transition: none !important;
  opacity: calc(var(--opacity-hovered) * var(--opacity-roman-ratio)) !important;
}

/* ─ C ─ [data-sync="none"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="none"]:hover .blyrics--line:not(:hover) {
  transform: scale(1) !important;
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line.blyrics--line.blyrics--line:hover {
  transform: scale(1.1) !important;
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line:hover .blyrics--word {
  opacity: var(--opacity-hovered) !important;
  transition: none !important;
}

#blyrics-wrapper .blyrics-container[data-sync="none"]:hover .blyrics--line:not(.blyrics--active) {
  opacity: var(--opacity-scrolling);
}

#blyrics-wrapper .blyrics-container[data-sync="none"]:hover .blyrics--line:not(.blyrics--active):hover {
  opacity: var(--opacity-hovered);
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line:hover .blyrics--translated {
  opacity: calc(var(--opacity-hovered) * var(--opacity-trans-ratio));
}

#blyrics-wrapper .blyrics-container[data-sync="none"] .blyrics--line:hover .blyrics--romanized {
  opacity: calc(var(--opacity-hovered) * var(--opacity-roman-ratio));
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 5 — SCROLLING (.blyrics-user-scrolling)
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

/* ─ A ─ [data-sync="richsync"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="richsync"].blyrics-user-scrolling {
  --opacity-words-past-line: var(--opacity-scrolling);
  --opacity-words-future-line: var(--opacity-scrolling);
  --opacity-past-word-current-line: var(--opacity-scrolling);
  --opacity-future-word-current-line: var(--opacity-scrolling);
  --opacity-current-word-current-line: 1;
}

#blyrics-wrapper .blyrics-container[data-sync="richsync"].blyrics-user-scrolling .blyrics--line {
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

/* ─ B ─ [data-sync="synced"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="synced"].blyrics-user-scrolling {
  --opacity-line-past: var(--opacity-scrolling);
  --opacity-line-future: var(--opacity-scrolling);
}

#blyrics-wrapper .blyrics-container[data-sync="synced"].blyrics-user-scrolling .blyrics--line {
  transition-property: transform !important;
  transition-duration: .2s !important;
  transition-timing-function: ease !important;
  transition-delay: 0s !important;
}

/* ─ C ─ [data-sync="none"] ───────────────────── */

#blyrics-wrapper .blyrics-container[data-sync="none"].blyrics-user-scrolling .blyrics--line {
  opacity: var(--opacity-unsynced);
}

/* ++++++++++++++++++++++++++++++++++++++++++++++++++
   SECTION 6 - OTHER STUFF (non-sync-type specific)
   ++++++++++++++++++++++++++++++++++++++++++++++++++ */

.blyrics--instrumental-icon {
  display: none;
}

ytmusic-player-page:before {
  background: black;
  filter: none;
}

@keyframes blyrics-shimmer {
  0%   { opacity: 1; }
  100% { opacity: 0.5; }
}

@keyframes blyrics-spin {
  0%, 100% { transform: none; }
}

#blyrics-loader::before {
  animation: blyrics-shimmer 1s infinite alternate ease-in-out;
}

#blyrics-loader::after {
  color: var(--blyrics-ui-text-color);
  white-space: pre-wrap;
}

#blyrics-loader:not([state="showing-message"]):not([state="exiting-message"])::after {
  background: unset;
  -webkit-background-clip: unset;
  background-clip: unset;
  color: var(--blyrics-ui-text-color);
  animation: blyrics-shimmer 1s infinite alternate ease-in-out;
  will-change: unset;
}

#blyrics-loader {
  visibility: visible !important;
  transition:
    opacity var(--blyrics-loader-transition-duration, 0.6s) var(--blyrics-loader-transition-easing),
    transform var(--blyrics-loader-transition-duration, 0.6s) var(--blyrics-loader-transition-easing),
    display var(--blyrics-loader-transition-duration, 0.6s) allow-discrete !important;
}

#blyrics-loader[state="hidden"],
#blyrics-loader:not([state]) {
  display: none !important;
  opacity: 0 !important;
  transform: translateY(0) scale(0.85) !important;  
}

#blyrics-loader[state="full-loader"],
#blyrics-loader[state="small-loader"],
#blyrics-loader[state="showing-message"],
#blyrics-loader[state="exiting"],
#blyrics-loader[state="exiting-message"] {
  display: flex !important;
}

@starting-style {
  #blyrics-loader[state="full-loader"],
  #blyrics-loader[state="small-loader"],
  #blyrics-loader[state="showing-message"],
  #blyrics-loader[state="exiting"],
  #blyrics-loader[state="exiting-message"] {
    opacity: 0;
    transform: translateY(0) scale(0.85);
  }
}

#blyrics-ad-overlay {
  display: none !important;
  opacity: 0;
  animation-play-state: paused;
  transition:
    opacity 0.4s var(--blyrics-loader-transition-easing),
    transform 0.4s var(--blyrics-loader-transition-easing),
    animation-play-state 0.4s allow-discrete
    display 0.4s allow-discrete !important;
}

#blyrics-ad-overlay[active],
:has(ytmusic-player-bar[is-advertisement]) #blyrics-ad-overlay {
  display: flex !important;
  opacity: 1;
}

@starting-style {
  #blyrics-ad-overlay[active],
  :has(ytmusic-player-bar[is-advertisement]) #blyrics-ad-overlay {
    opacity: 0;
    transform: translateY(0) scale(0.85);
  }
}

#blyrics-ad-overlay::before {
  animation: blyrics-shimmer 1s infinite alternate ease-in-out;
}

#blyrics-ad-overlay::after {
  background: none;
  -webkit-background-clip: unset;
  background-clip: unset;
  color: var(--blyrics-ui-text-color);
  white-space: pre-wrap;
  animation: blyrics-shimmer 1s infinite alternate ease-in-out;
  will-change: unset;
}

#blyrics-wrapper {
  transition:
    transform 0.2s var(--transition-curve),
    opacity 0.2s var(--transition-curve),
    display 0.2s allow-discrete !important;
}

:has(ytmusic-player-bar[is-advertisement]) #blyrics-wrapper,
:has(ytmusic-player-bar[is-advertisement]) #blyrics-loader {
  display: none !important;
  opacity: 0 !important;
  transform: translateY(0) scale(0.85) !important;
}

@starting-style {
  #blyrics-wrapper {
    opacity: 0;
    transform: translateY(0) scale(0.85);
  }
}

  /*# SECTION-6.2 - Footer #########################*/

.blyrics-footer__container.blyrics-footer__shaders {
    animation: none;
    background: var(--blyrics-footer-bg-color);
    box-shadow: none;
    border: 1px solid var(--blyrics-footer-border-color);
    padding: 0.5rem 1.25rem;
    font-weight: unset; 

    & > span {
        animation: none;
        background: none;
        -webkit-background-clip: unset;
        color: var(--blyrics-footer-link-color);
    }

    &:hover > span {
        text-decoration: underline;
    }
}

#blyrics-wrapper#blyrics-wrapper > .blyrics-container .blyrics-footer {
    opacity: 1;

    & > * {
        opacity: 0.3;
        transition: opacity 0.1s var(--transition-curve, ease); 

        &:hover {
            opacity: 1;
        }
    }
}

@if $enable-minimal == on {


/* 

#   #  #####  #   #  #####  #   #   ###   #    
## ##    #    ##  #    #    ## ##  #   #  #    
# # #    #    # # #    #    # # #  #####  #    
#   #    #    #  ##    #    #   #  #   #  #    
#   #  #####  #   #  #####  #   #  #   #  #####

*/


/* ## SECTION-0 - Info and Intro ################## */
/* 2026-08-12 v1.8.3 (BetterLyrics v2.4.0+ only)    */

/* The following is extracted from Minimal and      */
/*   modified to work with this theme. some things  */
/*   may not work the same.                         */

/* Section 3.3 has all the set background effects.  */

/* Lower blur value gives higher performance.       */

/* The background can be can be completely disabled */
/*   from extension settings for better performance.*/

/*     ____Time, Tide, & I wait for nothing____     */
/*               __ - boidu (maybe)__               */


/* ## SECTION-2 - WEBSITE-WIDE FONT SETTINGS ###### */
* {
  font-optical-sizing: auto;
  scrollbar-width: thin;
}

/* ## SECTION-3 - EASY CHANGE SETTINGS ############ */
:root {
  /*# SECTION-3.1 ################################# */

  --blyrics-padding: 0.45em; 

  /*# SECTION-3.2 ################################# */


  /*# SECTION-3.3 Background Effects ############## */
  --blyrics-gradient-stops: none;
  --background-image-transition: background 2.5s ease-in-out 5s;

  @if $enable-blur == on {
      --blyrics-background-blur: blur(#{$blur-amount});
      --background-image-filter: saturate(var(--blyrics-background-saturate))
  brightness(var(--background-brightness)) contrast(var(--background-contrast))
  var(--blyrics-background-blur);
  } @else {
      --blyrics-background-blur: none;
      --background-image-filter: saturate(var(--blyrics-background-saturate))
  brightness(var(--background-brightness)) contrast(var(--background-contrast));
  }

  --blyrics-background-saturate: 1;
  --background-brightness: 0.20;
  --background-contrast: 0.85;
  --filter-replacement-backgroundcolor: oklch(18.67% 0.0000 0 / 0.80);
  
  /*# SECTION-3.4 Custom Variables ################ */
 /* --transition-curve: cubic-bezier(0.27, 1.06, 0.18, 1.00); */
  --white-1:      oklch(1 0 0 / 1);
  --white-dot-87: oklch(1 0 0 / 0.87);
  --white-dot-60: oklch(1 0 0 / 0.60);
  --white-dot-15: oklch(1 0 0 / 0.15);
  --white-dot-10: oklch(1 0 0 / 0.10);
  --black-dot-87: oklch(0 0 0 / 0.87);
  --black-dot-60: oklch(0 0 0 / 0.60);
/*  --blyrics-loader-transition-easing: cubic-bezier(0.34, 1.3, 0.64, 1); */
  
  
  /* ## End of SECTION-3.4 ######################## */

}


/* ## End of SECTION-3 ############################ */



/* ## SECTION-4 - Lyric Animation Fine-tuning ##### */



/* ## SECTION-5 - Hiding Scrollbar from homepage ## */
html {
  scrollbar-width: none;
}
ytmusic-app-layout > [slot="player-bar"] {
  width: 100dvw;
}

/* ## SECTION-6 - Style and animation of lyrics ### */

  /* ## SECTION-6.1 - Lyrics and wrapper ########## */

ytmusic-app-layout:not([is-mweb-modernization-enabled])
  [player-fullscreened]:not([blyrics-dfs])
  .blyrics-container {
  font-size: 4.5rem;
}
 
  /*# SECTION-6.2 - Footer #########################*/

.blyrics-footer__container.blyrics-footer__shaders {
    animation: none;
    background: var(--blyrics-footer-bg-color);
    box-shadow: none;
    border: 1px solid var(--blyrics-footer-border-color);
    padding: 0.5rem 1.25rem;
    font-weight: unset; 

    & > span {
        animation: none;
        background: none;
        -webkit-background-clip: unset;
        color: var(--blyrics-footer-link-color);
    }

    &:hover > span {
        text-decoration: underline;
    }
}

#blyrics-wrapper#blyrics-wrapper > .blyrics-container .blyrics-footer {
    opacity: 1;

    & > * {
        opacity: 0.3;
        transition: opacity 0.1s var(--transition-curve, ease); 

        &:hover {
            opacity: 1;
        }
    }
}

/* ## SECTION-7 - Before Lyrics are Loaded ######## */   
ytmusic-player-page::before {
  background: var(
    --blyrics-background-img,
    var(--ytmusic-brand-background-solid)
  );
  background-position: 50%;
  background-repeat: no-repeat;
  background-size: cover;
  content: "";
  filter: var(--background-image-filter);
  height: 100dvh;
  inset: calc(0px - var(--ytmusic-nav-bar-height)) 0 0
    calc(0px - var(--ytmusic-guide-width));
  position: fixed;
  transition: var(--background-image-transition);
  width: 100dvw;
}
ytmusic-player-page[is-mweb-modernization-enabled]::before {
  inset: 0;
}
ytmusic-player-page[player-fullscreened]::before {
  inset: calc(0px - var(--ytmusic-player-bar-height)) 0 0 0;
}

/* ## SECTION-9 - Album Art Effects ############### */
#player.ytmusic-player-page,
.image.ytmusic-player-bar {
  border-radius: 8% !important;
  corner-shape: superellipse(2);
  box-shadow: none !important;
  overflow: hidden;
}

#player.ytmusic-player-page {
  background: transparent;
}

#song-image,
#thumbnail,
#thumbnail img,
#blyrics-img {
  border-radius: 8%;
  corner-shape: superellipse(2);
  overflow: hidden;
}

/* ## SECTION-10 - Portrait Window Lyrics Screen ## */
ytmusic-player-page[player-fullscreened] .song-media-controls.ytmusic-player {
  background-image: none !important;
}
.blyrics-container,
.blyrics-container::after,
.blyrics-container::before,
[is-mweb-modernization-enabled][page-type="MUSIC_PAGE_TYPE_TRACK_LYRICS"] {
  background: none !important;
  filter: none !important;
}
ytmusic-player-page[is-mweb-modernization-enabled]
  .background-gradient.ytmusic-player-page,
ytmusic-player-page[is-mweb-modernization-enabled]
  .background-thumbnail.ytmusic-player-page {
    display: none;
}

/* ## SECTION-11 - Portrait Window Lyrics Background*/
ytmusic-player-page[is-mweb-modernization-enabled]
  .background-image.ytmusic-player-page,
ytmusic-player-page[is-mweb-modernization-enabled]
  .side-panel.ytmusic-player-page {
  background: none;
}

ytmusic-player-page[is-mweb-modernization-enabled]
  .background-gradient.ytmusic-player-page,
ytmusic-player-page[is-mweb-modernization-enabled]
  .background-thumbnail.ytmusic-player-page {
  display: none;
}

ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  #side-panel.ytmusic-player-page,
ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  #top-player-bar.ytmusic-player-page {
  backdrop-filter: none;
  background: transparent !important;
  border: 0;
}

#tab-renderer[page-type="MUSIC_PAGE_TYPE_TRACK_LYRICS"],
[is-mweb-modernization-enabled] #player-bar-background.ytmusic-app-layout,
[is-mweb-modernization-enabled] .blyrics-container,
[is-mweb-modernization-enabled][page-type="MUSIC_PAGE_TYPE_TRACK_LYRICS"],
ytmusic-app-layout .ytmusic-app[is-mweb-modernization-enabled] {
  backdrop-filter: none;
  background: none !important;
  filter: none !important;
  border: 0;
}

[is-mweb-modernization-enabled] .blyrics-container::after,
[is-mweb-modernization-enabled] .blyrics-container::before {
  opacity: 0;
}

[is-mweb-modernization-enabled][player-page-open] #guide,
ytmusic-app-layout[is-mweb-modernization-enabled][player-page-open]
  > [slot="nav-bar"] {
  display: none;
}

/* ## SECTION-12 - Player bar background ########## */
ytmusic-player-bar {
  background: transparent !important;

  &:not([player-fullscreened]) {
    backdrop-filter: none !important;
  }
}

#player-bar-background {
  overflow: hidden;
  background-color: transparent;

  &.ytmusic-app-layout {
    background: transparent;
  }
}

ytmusic-app-layout {
  &:not([player-ui-state="FULLSCREEN"]) #player-bar-background {
    backdrop-filter: none !important;
  }

  &[player-ui-state="PLAYER_BAR_ONLY"] #player-bar-background::before {
    content: "";
    position: absolute;
    inset: -90px;
    background: var(
      --blyrics-background-img,
      var(--ytmusic-brand-background-solid)
    );
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    transition: var(--background-image-transition);
    filter: var(--background-image-filter);
    z-index: 0;
  }

  &[player-ui-state="PLAYER_PAGE_OPEN"] #player-bar-background::before {
    content: "";
    position: absolute;
    inset: -90px;
    background: var(
      --blyrics-background-img,
      var(--ytmusic-brand-background-solid)
    );
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    transition: opacity 0.3s ease-in-out 0.3s;
    filter: var(--background-image-filter);
    z-index: 0;
    opacity: 0;
  }

  &:not([player-ui-state="PLAYER_BAR_ONLY"]) #player-bar-background::before {
   /* background: transparent !important; */
   
  }
}

#layout[player-ui-state="PLAYER_PAGE_OPEN"] #player-bar-background {
    opacity: 1 !important;
}

ytmusic-app[is-bauhaus-sidenav-enabled] #guide-wrapper.ytmusic-app {
  height: calc(100vh - var(--ytmusic-player-bar-height));
}

/* ## SECTION-13 - Bottom Left Nofication ######### */
tp-yt-paper-toast #close-button {
  display: none !important;
}

#toast {
  background-color: var(--ytmusic-brand-background-solid);
  border: 1.5px solid var(--white-dot-10);
  border-radius: 20.88px !important;
  corner-shape: superellipse(2);
}

.yt-spec-button-shape-next--call-to-action-inverse.yt-spec-button-shape-next--text {
  color: oklch(0.63 0.19 257.14);
}

.yt-spec-button-shape-next--call-to-action-inverse.yt-spec-button-shape-next--text:hover {
  background: oklch(0.63 0.19 257.14 / 0.13);
}

yt-notification-action-renderer #toast:not(:hover) {
  animation: idontwannaseeyouanymore-billie 2.5s ease 2.5s forwards !important;
}

yt-notification-action-renderer #toast:hover {
  animation: none !important;
  opacity: 1 !important;
  transform: translateY(0) !important;
  transition:
    opacity 0.3s var(--transition-curve),
    transform 0.3s var(--transition-curve) !important;
}

@keyframes idontwannaseeyouanymore-billie {
  0% {
    opacity: 1;
    transform: translateY(0);
    animation-timing-function: ease-in;
  }
  87% {
    opacity: 0.6;
    transform: translateY(0);
    pointer-events: auto;
    animation-timing-function: ease;
  }
  99% {
    opacity: 0;
    transform: translateY(100px);
    pointer-events: auto;
  }
  100% {
    opacity: 0;
    transform: translateY(100px);
    pointer-events: none;
    visibility: hidden;
  }
}

#text.yt-notification-action-renderer, #sub-text.yt-notification-action-renderer {
    color: var(--white-1);
    opacity: 0.87;
}

/* ## SECTION-14 - Volume popup ################### */
ytmusic-player-expanding-menu {
  border-radius: 13.92px;
  corner-shape: superellipse(2);
  max-height: 60px;
}

#main-panel.ytmusic-player-page {
  height: auto;
}

/* ## SECTION-15 - Space above album art ########## */
.strapline.ytmusic-responsive-header-renderer {
  margin-top: 0;
}

/* ## SECTION-16 - Background of Albums etc. ###### */
ytmusic-fullbleed-thumbnail-renderer {
  display: block !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100dvw !important;
  height: 100dvh !important;
  max-width: none !important;
  max-height: none !important;
  z-index: 0;
}

ytmusic-app-layout[is-bauhaus-sidenav-enabled].content-scrolled #mini-guide-background.ytmusic-app-layout {
  opacity: 0;
}

#layout[player-ui-state="PLAYER_PAGE_OPEN"] #nav-bar-background {
  transition: opacity 0.4s ease-in-out 0.2s;
}

#background > ytmusic-fullbleed-thumbnail-renderer > picture::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: var(--filter-replacement-backgroundcolor);
  pointer-events: none;
  backdrop-filter: var(--blyrics-background-blur);
}

ytmusic-browse-response[has-background][page-type="MUSIC_PAGE_TYPE_NON_MUSIC_AUDIO_TRACK_PAGE"]
  .immersive-background.ytmusic-browse-response
  ytmusic-fullbleed-thumbnail-renderer.ytmusic-browse-response,
ytmusic-browse-response[has-background][page-type="MUSIC_PAGE_TYPE_PODCAST_SHOW_DETAIL_PAGE"]
  .immersive-background.ytmusic-browse-response
  ytmusic-fullbleed-thumbnail-renderer.ytmusic-browse-response,
ytmusic-browse-response[has-background][is-playlist-detail-page]
  .immersive-background.ytmusic-browse-response
  ytmusic-fullbleed-thumbnail-renderer.ytmusic-browse-response,
ytmusic-browse-response[has-background][is-album-detail-page]
  .immersive-background.ytmusic-browse-response
  ytmusic-fullbleed-thumbnail-renderer.ytmusic-browse-response {
  filter: none;
}

ytmusic-browse-response[has-background]:not([disable-gradient])
  .background-gradient.ytmusic-browse-response {
  background-image: none;
}

.image.ytmusic-immersive-header-renderer
  ~ .content-container-wrapper.ytmusic-immersive-header-renderer
  .gradient-container.ytmusic-immersive-header-renderer {
  background: none;
}

.image.ytmusic-immersive-header-renderer
  ~ .content-container-wrapper.ytmusic-immersive-header-renderer {
  background: none;
}

#blyrics-wrapper > div > div.blyrics-footer {
  opacity: 1;
}

/* ## SECTION-17 - Search Box ##################### */
ytmusic-search-box {
  &[is-bauhaus-sidenav-enabled] {
    --ytmusic-search-border: var(--white-dot-10);
    --ytmusic-search-background: oklch(from #212121 l c h);

    & .search-box.ytmusic-search-box {
      transition: background-color 0.2s var(--transition-curve);
    }

    & #suggestion-list {
      transition-duration: 0.1s;
      transition-property: opacity;
      transition-timing-function: var(--transition-curve);

      &.ytmusic-search-box {
        border-radius: 0 0 20px 20px;
        corner-shape: superellipse(2);
      }
    }

    &:not([opened]) {
      & .search-container.ytmusic-search-box {
        border-radius: 20px;
        corner-shape: superellipse(2);
      }

      & #suggestion-list {
        opacity: 0;
      }

      &:not([has-query]) {
        opacity: 0.7;
        transition: opacity 0.2s var(--transition-curve);

        &:hover {
          opacity: 1;
        }

        & .search-box.ytmusic-search-box {
          background: var(--white-dot-10);
          border-radius: 20px;
          corner-shape: superellipse(2);
        }
      }
    }

    &[has-query] .search-container.ytmusic-search-box {
      box-shadow: none;
    }

    &[opened]:not(:has(ytmusic-search-suggestions-section)) {
      & #suggestion-list {
        opacity: 0;
      }

      & .search-box.ytmusic-search-box {
        border-radius: 20px;
        corner-shape: superellipse(2);
      }
    }

    &[opened]:has(ytmusic-search-suggestions-section) .search-box.ytmusic-search-box {
      border-radius: 20px 20px 0 0;
      corner-shape: superellipse(2);
    }
  }

  &[opened] .search-container.ytmusic-search-box {
    box-shadow: none;
  }
}

ytmusic-search-suggestion {
  color: var(--white-dot-87);
}

#suggestions.ytmusic-search-suggestions-section
  > .ytmusic-search-suggestions-section {
  border-radius: 20px;
  corner-shape: superellipse(2);
  
  &:is(.hover, .selected-suggestion, :hover) {
    background-color: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
    border-radius: 20px;
    corner-shape: superellipse(2);
  }
}

/* ## SECTION-18 - 3button menu design ############ */
ytmusic-menu-popup-renderer {
  display: flex;
  flex-direction: column;
  overflow: hidden !important;
}

tp-yt-paper-listbox.ytmusic-menu-popup-renderer {
  width: auto;
  padding: 1.2em 0;
  border-radius: 13.92px;
  corner-shape: superellipse(2);
}

tp-yt-paper-listbox#items {
  flex: 1 1 auto;
  overflow-y: auto;
  scroll-behavior: smooth;
}

ytmusic-popup-container tp-yt-iron-dropdown {
  width: auto !important;
  height: auto !important;
  max-width: none !important;
  background: transparent !important;
  box-shadow: none !important;
  pointer-events: none !important;
}

ytmusic-popup-container #contentWrapper {
  display: flex !important;
  flex-direction: column !important;
  justify-content: flex-start !important;
  width: max-content !important;
  max-height: 65dvh !important;
  pointer-events: auto !important;
}

#items.ytmusic-menu-popup-renderer > .ytmusic-menu-popup-renderer {
  border-radius: 13.92px;
  corner-shape: superellipse(2);
  
  &:hover {
    background-color: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
    border-radius: 13.92px;
    corner-shape: superellipse(2);
   }
}

/* ## SECTION-19 - Player Queue Header ############ */
ytmusic-queue-header-renderer {
  box-sizing: border-box;
  border-radius: 5cqi;
  corner-shape: superellipse(2);
  align-items: flex-start;
  padding: 2.5cqi;
  margin: 0 0cqi 2cqi;
  position: sticky;
  top: 2cqi;
  z-index: 1000;
  container-type: inline-size;
  border: none;
  box-shadow: 0px 4px 10px 0px oklch(0 0 0 / 0.2);

  &::before {
    content: "";
    overflow: hidden;
    position: absolute;
    background: var(
      --blyrics-background-img,
      var(--ytmusic-brand-background-solid)
    );
    background-position: 50%;
    background-repeat: no-repeat;
    background-size: cover;
    transition: var(--background-image-transition);
    filter: brightness(var(--background-brightness))
      contrast(var(--background-contrast));
    z-index: -1;
    inset: 0;
    border: none;
    border-radius: inherit;
    corner-shape: inherit;
    opacity: 1;
  }

  &::after {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    corner-shape: inherit;
    border: none;
    /*box-shadow: inset 0 0 0px 1px white;*/
    outline: 1px solid white;
    outline-offset: -1px;
    opacity: 0.1;
    z-index: -1;
    pointer-events: none;
  }

  .container-name {
    & .subtitle,
    & .title {
      color: var(--white-dot-87);
    }

    & .title {
      font-weight: 100;
    }
  }
}

ytmusic-chip-cloud-chip-renderer[chip-style="STYLE_TRANSPARENT"] {
  & a.ytmusic-chip-cloud-chip-renderer {
    border-radius: 2.5cqi;
    corner-shape: superellipse(2);
    background: var(--white-dot-60);
    padding: 6px;
    color: var(--black-dot-87);

    yt-icon {
      color: inherit;
    }

    &:hover {
      background-color: var(--white-dot-87);
    }
  }
}

#buttons > ytmusic-chip-cloud-chip-renderer > div > a > yt-icon {
  margin-left: 5px;
}

#tab-renderer {
  &[page-type="MUSIC_PAGE_TYPE_TRACK_RELATED"] {
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 8%);
    mask-image: linear-gradient(to bottom, transparent 0%, black 8%);  
  }

  &:not([page-type="MUSIC_PAGE_TYPE_TRACK_RELATED"]):not(
      [page-type="MUSIC_PAGE_TYPE_TRACK_LYRICS"]
    ) {
    margin-top: 0;
  }
}

.scroller.ytmusic-player-page {
  -webkit-mask-image: linear-gradient(to bottom, transparent 0, oklch(1 0 0/0.2) 0.5cqi, black 2.5cqi);
  mask-image: linear-gradient(to bottom, transparent 0, oklch(1 0 0/0.2) 0.5cqi, black 2.5cqi);
  mask-attachment: scroll;
  -webkit-mask-attachment: scroll;
}

/* ## SECTION-20 - Player Queue ################### */
ytmusic-player-queue-item[play-button-state="loading"],
ytmusic-player-queue-item[play-button-state="paused"],
ytmusic-player-queue-item[play-button-state="playing"] {
  border-radius: 13.92px;
  corner-shape: superellipse(2);
}

/* ## SECTION-21 - Player Queue Items ############# */
ytmusic-player-queue-item {
  border: none;
  border-radius: 13.92px;
  corner-shape: superellipse(2);
}

#contents ytmusic-player-queue-item {
&:hover {
    background: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }
}

/* ## SECTION-22 - Player Queue- dragging item #### */
ytmusic-player-queue-item:is(
    [style*="z-index: 9"],
    [style*="cursor: move"]
  ):hover {
  border-radius: 13.92px !important;
  corner-shape: superellipse(2);
  background: var(--ytmusic-brand-background-solid, #030303) !important;
  box-sizing: border-box !important;
  height: var(--ytmusic-list-item-height, 56px) !important;
  width: auto !important;
  margin: 0 !important;
}
ytmusic-playlist-panel-video-wrapper-renderer:is(
    [style*="z-index: 9"],
    [style*="cursor: move"]
  ) {
  box-sizing: border-box !important;
  height: var(--ytmusic-list-item-height, 56px) !important;
  width: auto !important;
  margin: 0 !important;
  background: var(--ytmusic-brand-background-solid, #030303) !important;
  border-radius: 13.92px;
  corner-shape: superellipse(2);
  display: flex !important;
  align-items: center;
  padding: 0 8px;
}
ytmusic-playlist-panel-video-wrapper-renderer:is(
    [style*="z-index: 9"],
    [style*="cursor: move"]
  ):hover {
  box-sizing: border-box !important;
  height: var(--ytmusic-list-item-height, 56px) !important;
  width: auto !important;
  margin: 0 !important;
  background: var(--ytmusic-brand-background-solid, #030303) !important;
  border-radius: 13.92px;
  corner-shape: superellipse(2);
  display: flex !important;
  align-items: center;
  padding: 0 8px;
}

/* ## SECTION-23 - Arist Page background ########## */
ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
  .image.ytmusic-fullbleed-thumbnail-renderer {
  filter: none !important;
  opacity: 1 !important;
}
ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
  ytmusic-fullbleed-thumbnail-renderer::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 200dvh;
  --fade-height: calc(
    var(--ytmusic-immersive-header-padding-top) +
      var(--ytmusic-immersive-header-gradient-padding-top)
  );
  --mask-gradient: linear-gradient(
  to bottom in oklch,
  oklch(0 0 0 / 0.000) 0dvh,
  oklch(0 0 0 / 0.009) calc(var(--fade-height) * 1 / 6),
  oklch(0 0 0 / 0.040) calc(var(--fade-height) * 2 / 6),
  oklch(0 0 0 / 0.098) calc(var(--fade-height) * 3 / 6),
  oklch(0 0 0 / 0.184) calc(var(--fade-height) * 4 / 6),
  oklch(0 0 0 / 0.301) calc(var(--fade-height) * 5 / 6),
  oklch(0 0 0 / 0.450) calc(var(--fade-height) * 6 / 6),
  oklch(0 0 0 / 0.590) calc(var(--fade-height) * 7 / 6),
  oklch(0 0 0 / 0.708) calc(var(--fade-height) * 8 / 6),
  oklch(0 0 0 / 0.804) calc(var(--fade-height) * 9 / 6),
  oklch(0 0 0 / 0.880) calc(var(--fade-height) * 10 / 6),
  oklch(0 0 0 / 0.936) calc(var(--fade-height) * 11 / 6),
  oklch(0 0 0 / 0.974) calc(var(--fade-height) * 12 / 6),
  oklch(0 0 0 / 0.994) calc(var(--fade-height) * 13 / 6),
  oklch(0 0 0 / 1.000) calc(var(--fade-height) * 14 / 6)
);
  -webkit-mask-image: var(--mask-gradient);
  mask-image: var(--mask-gradient);
  background-color: var(--filter-replacement-backgroundcolor);
  backdrop-filter: var(--blyrics-background-blur);
  z-index: 1;
  pointer-events: none;
  animation: slide-dimmer-up linear both;
  animation-timeline: scroll(root);
  animation-range: 0 50dvh;
}
@keyframes slide-dimmer-up {
  0% {
    transform: translateY(0);
  }
  to {
    transform: translateY(-100dvh);
  }
}
ytmusic-immersive-header-renderer {
  --ytmusic-immersive-header-padding-top: 48dvh;
  --ytmusic-immersive-header-gradient-padding-top: 12dvh;
}

/* ## SECTION-23.1 - Portrait Window Specifics #### */
[is-mweb-modernization-enabled] ytmusic-immersive-header-renderer {
  --ytmusic-immersive-header-padding-top: 28dvh;
  --ytmusic-immersive-header-gradient-padding-top: 12dvh;
}

@property --fade-stop {
  syntax: '<percentage>';
  inherits: false;
  initial-value: 65%;
}

[is-mweb-modernization-enabled]
  ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
  .image.ytmusic-fullbleed-thumbnail-renderer.ytmusic-fullbleed-thumbnail-renderer {
  position: fixed !important;
  top: 0;
  left: 0;
  z-index: 0;
  width: 100dvw;
  height: 45dvh;
  object-fit: cover;
  --fade-stop: 65%;
  mask-image: linear-gradient(
    to bottom in oklch,
    black var(--fade-stop),
    calc(var(--fade-stop) + (100% - var(--fade-stop)) * 0.65),
    transparent 100%
  );
  -webkit-mask-image: linear-gradient(
    to bottom in oklch,
    black var(--fade-stop),
    calc(var(--fade-stop) + (100% - var(--fade-stop)) * 0.65),
    transparent 100%
  );
  animation-name: expand-dim-stick;
  animation-timeline: scroll(root);
  animation-range: 0dvh 70dvh;
  animation-fill-mode: both;
  animation-timing-function: ease-out;
}

[is-mweb-modernization-enabled]
  ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
  .image.ytmusic-fullbleed-thumbnail-renderer
  img,
[is-mweb-modernization-enabled]
  ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
  .image.ytmusic-fullbleed-thumbnail-renderer
  picture {
  width: 100dvw !important;
  height: 100dvh !important;
  object-fit: cover !important;
}

@keyframes expand-dim-stick {
  90% {
    height: 100dvh;
    --fade-stop: 90%;
  }

  to {
    height: 100dvh;
    --fade-stop: 100%;
  }
}

@media (prefers-reduced-motion: reduce) {
  ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
    ytmusic-fullbleed-thumbnail-renderer::before {
    animation: none;
    transform: translateY(-100dvh);
  }

  [is-mweb-modernization-enabled]
    ytmusic-browse-response:has([main-page-type="MUSIC_PAGE_TYPE_ARTIST"])
    .image.ytmusic-fullbleed-thumbnail-renderer.ytmusic-fullbleed-thumbnail-renderer {
    animation: none;
    height: 100dvh;
    --fade-stop: 100%;
  }
}

/* ## SECTION-24 - Fullscreen No Lyrics animation #*/
ytmusic-player-page[player-fullscreened]:not([blyrics-video-mode]):not(
    [blyrics-dfs]
  ):has(.blyrics-container[data-no-lyrics="true"])
  #main-panel {
    transition: transform 1.3s ease-in-out var(--blyrics-no-lyrics-slide-delay);
}

ytmusic-player-page[player-fullscreened][blyrics-video-mode]:not(
    [blyrics-dfs]
  ):has(.blyrics-container[data-no-lyrics="true"])
  #main-panel {
    transition: transform 1.3s ease-in-out var(--blyrics-no-lyrics-slide-delay);
}

ytmusic-player-page[player-fullscreened]:not([blyrics-video-mode]):not(
    [blyrics-dfs]
  ):has(.blyrics-container[data-no-lyrics="true"])
  #player {
    transition: max-width 1.3s ease-in-out var(--blyrics-no-lyrics-slide-delay);
}

ytmusic-player-page[player-fullscreened]:not([blyrics-dfs])
  #blyrics-wrapper:has(.blyrics-container[data-no-lyrics="true"]) {
    transition: opacity 1.3s ease-in-out var(--blyrics-no-lyrics-slide-delay);
}

/* ## SECTION-25 - Music Note Animation disable ### */ 
ytmusic-player#player,
ytmusic-player-bar,
#top-player-bar {
  view-transition-name: none !important;
}

@keyframes open-player-page {
  0% {
    transform: translate3d(0, 100vh, 0);
    opacity: 1;
  }
  100% {
    transform: translate3d(0, 0, 0);
    opacity: 1;
  }
}

@keyframes collapse-player-page {
  0% {
    transform: translate3d(0, 0, 0);
    opacity: 1;
  }
  99% {
    transform: translate3d(0, 100vh, 0);
    opacity: 1;
  }
  100% {
    transform: translate3d(0, 100vh, 0);
    opacity: 0;
  }
}

ytmusic-app-layout[player-page-open] > [slot="player-page"] {
  animation: open-player-page 0.4s ease-in-out !important;
}

ytmusic-app-layout[player-ui-state="PLAYER_BAR_ONLY"] > [slot="player-page"] {
  animation: collapse-player-page 0.4s ease-in-out !important;
}

ytmusic-player-page[player-ui-state="PLAYER_BAR_ONLY"] ytmusic-player#player {
  transition-property:
    position, bottom, block-size, height, inline-size, width, left, margin,
    right, top, border-radius, transform, max-width !important;
  transition-behavior: allow-discrete !important;
  transition-delay: 0.4s !important;
  transition-duration: 0.0013s !important;
  transition-timing-function: step-end !important;
}

ytmusic-player-page[player-ui-state="PLAYER_BAR_ONLY"]
  ytmusic-player#player
  .song-media-controls.ytmusic-player {
  transition-property: display, opacity, visibility !important;
  transition-behavior: allow-discrete !important;
  transition-delay: 0.4s !important;
  transition-duration: 0.0013s !important;
  transition-timing-function: step-end !important;
}

ytmusic-player-page[player-ui-state="PLAYER_BAR_ONLY"]
  #top-player-bar.ytmusic-player-page {
  opacity: 0 !important;
  visibility: hidden !important;
  transition: none !important;
}

ytmusic-player-page[player-ui-state="PLAYER_BAR_ONLY"]
  tp-yt-paper-tabs.ytmusic-player-page {
  visibility: hidden !important;
  transition-behavior: allow-discrete !important;
  transition: visibility 0.4s step-end !important;
}

#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="FULL_PLAYER_VIEW"]
  #info-panel.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="FULL_PLAYER_VIEW"]
  #player-controls.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="FULL_PLAYER_VIEW"]
  .av.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="FULL_PLAYER_VIEW"]
  .collapse-button.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="FULL_PLAYER_VIEW"]
  .context-menu-button.ytmusic-player-page {
  opacity: 1;
  visibility: visible;
  transform: none;
  transition:
    opacity 0.2667s ease-in-out 0.2667s,
    transform 0.4s ease-in-out,
    visibility 0s ease-in-out !important;
}

#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  #info-panel.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  #player-controls.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  .av.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  .collapse-button.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  .context-menu-button.ytmusic-player-page {
  opacity: 0 !important;
  visibility: hidden !important;
  transform: translate3d(0, -285%, 0);
  transition:
    opacity 0.1067s ease-in-out,
    transform 0.4s ease-in-out,
    visibility 0.4s ease-in-out !important;
}

#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-ui-state="PLAYER_BAR_ONLY"]
  #info-panel.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-ui-state="PLAYER_BAR_ONLY"]
  #player-controls.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-ui-state="PLAYER_BAR_ONLY"]
  .av.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-ui-state="PLAYER_BAR_ONLY"]
  .collapse-button.ytmusic-player-page,
#layout
  ytmusic-player-page[is-mweb-modernization-enabled][player-ui-state="PLAYER_BAR_ONLY"]
  .context-menu-button.ytmusic-player-page {
  opacity: 0 !important;
  visibility: hidden !important;
  transition:
    opacity 0.0667s ease-in-out 0.3333s,
    visibility 0.0667s ease-in-out 0.3333s !important;
}

::view-transition-group(player-bar),
::view-transition-old(player-bar),
#top-player-bar {
  transition-duration: 0.4s !important;
  transition-timing-function: ease-in-out !important;
}

#player,
#side-panel,
#main-panel {
  transition-property: transform !important;
  transition-duration: 0.4s !important;
  transition-timing-function: ease-in-out !important;
}

/* ## SECTION-27 - Tab headings animation change ## */
tp-yt-paper-ripple,
#selectionBar {
  display: none !important;
}

tp-yt-paper-tab {
  text-transform: uppercase;
  color: var(--white-1);
  opacity: 0.7;
  transition: none;

  &:hover {
    opacity: 1;
  }
}

tp-yt-paper-tab .tab-content {
  border-radius: 999rem;
  corner-shape: superellipse(1.2);
  transition: none;
}

[is-mweb-modernization-enabled] tp-yt-paper-tab .tab-content:hover,
tp-yt-paper-tab:not(.iron-selected) .tab-content:hover {
  color: var(--white-1);
  background-color: var(--white-dot-10);
  /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
}

[is-mweb-modernization-enabled][player-ui-state="MINIPLAYER_IN_PLAYER_PAGE"]
  tp-yt-paper-tab.iron-selected,
:not([is-mweb-modernization-enabled])[player-ui-state="PLAYER_PAGE_OPEN"]
  tp-yt-paper-tab.iron-selected {
  color: var(--white-1);
  opacity: 0.87;
  transition: none;

  &:hover {
    opacity: 1;
  }

  & .tab-content {
    background-color: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
  }

  & .tab-content:hover {
    background-color: var(--white-dot-15);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }
}

[is-mweb-modernization-enabled][player-ui-state="PLAYER_PAGE_OPEN"]
  tp-yt-paper-tab.iron-selected.ytmusic-player-page,
[is-mweb-modernization-enabled][player-ui-state="PLAYER_PAGE_OPEN"]
tp-yt-paper-tab:not(.iron-selected) .tp-yt-paper-tab[style-target=tab-content] {
  color: var(--white-1);
}

[player-ui-state="MINIPLAYER_IN_PLAYER_PAGE"]
  tp-yt-paper-tab.iron-selected
  .tab-content:hover {
  color: inherit;
  background: transparent;
  box-shadow: none;
}

.tp-yt-paper-tab[style-target=tab-content], tp-yt-paper-tab .tp-yt-paper-tab[style-target=tab-content] {
  transition: none;
}

ytmusic-player-page[is-mweb-modernization-enabled][player-page-ui-state="TABS_VIEW"]
  tp-yt-paper-tabs.ytmusic-player-page {
  border-bottom: none;
  margin-top: 15px;
}

ytmusic-player-page tp-yt-paper-tabs.ytmusic-player-page {
  border-bottom: none;
}

/* ### Section 28 - Portrait Window No Lyrics ##### */
ytmusic-player-page[is-mweb-modernization-enabled]
  #blyrics-wrapper
  > div.blyrics-no-lyrics-button-container {
  margin: 4rem;
}

ytmusic-player-page[is-mweb-modernization-enabled]
  #blyrics-wrapper
  > div.blyrics-container[data-no-lyrics="true"]
  #blyrics-spacing-element {
  display: none !important;
}

/* ### Section 29 - No Lyrics Text Replace ######## */
@keyframes fadeOutOnly {
  from { opacity: 1; }
  to { opacity: 0; }
}

@keyframes fadeInOnly {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes go-invisible {
  to {
    visibility: hidden;
    pointer-events: none;
  }
}

.blyrics-container[data-no-lyrics="true"] .blyrics--line {
  position: relative;
  min-height: 1lh;
}

.blyrics-container[data-no-lyrics="true"] .blyrics--line > span {
  animation:
    fadeOutOnly 2s forwards 8s,
    go-invisible 0s forwards 10s;
}

.blyrics-container[data-no-lyrics="true"]:hover .blyrics--line::after {
  content: "No lyrics found";
  transform: scale(1) translate(0px);
  transition: none;
}

.blyrics-container[data-no-lyrics="true"] {
  padding-top: 4em;
  padding-bottom: 4em;
}

.blyrics-container[data-no-lyrics="true"] .blyrics--line::after {
  content: "♫";
  position: absolute;
  inset: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  cursor: help;
  font-size: 1.4em;
  transform: scale(1);
  animation:
    fadeInOnly 2s forwards 10.2s,
    i-love-a-shooting-star 6s linear infinite 12.2s;
}

@keyframes i-love-a-shooting-star {
  0% {
    opacity: .45;
    transform: scale(1);
    text-shadow:
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 10px oklch(100% 0 0 / 0%);
  }
  25% {
    opacity: 1;
    transform: scale(1.5);
    text-shadow:
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 10px oklch(100% 0 0 / 0%);
  }
  26% {
    text-shadow:
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 12px oklch(100% 0 0 / 12%);
  }
  29% {
    text-shadow:
      -0.857em -0.6em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 16px oklch(100% 0 0 / 30%);
  }
  32% {
    text-shadow:
      -1.714em -1.2em 28.143px oklch(100% 0 0 / 11.429%),
      1.114em 0.043em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 20px oklch(100% 0 0 / 50%);
  }
  33% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      1.486em 0.057em 20.429px oklch(100% 0 0 / 34.286%),
      0.257em 0.257em 8.857px oklch(100% 0 0 / 68.571%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 22px oklch(100% 0 0 / 58%);
  }
  35% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.229em 0.086em 28.143px oklch(100% 0 0 / 11.429%),
      0.771em 0.771em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 26px oklch(100% 0 0 / 70%);
  }
  36% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.029em 1.029em 20.429px oklch(100% 0 0 / 34.286%),
      0.171em -0.329em 8.857px oklch(100% 0 0 / 68.571%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 28px oklch(100% 0 0 / 75%);
  }
  38% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.543em 1.543em 28.143px oklch(100% 0 0 / 11.429%),
      0.514em -0.986em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 32px oklch(100% 0 0 / 82%);
  }
  39% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      0.686em -1.314em 20.429px oklch(100% 0 0 / 34.286%),
      -0.386em 0.043em 8.857px oklch(100% 0 0 / 68.571%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 31px oklch(100% 0 0 / 80%);
  }
  41% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.029em -1.971em 28.143px oklch(100% 0 0 / 11.429%),
      -1.157em 0.129em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 27px oklch(100% 0 0 / 65%);
  }
  42% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -1.543em 0.171em 20.429px oklch(100% 0 0 / 34.286%),
      -0.214em 0.314em 8.857px oklch(100% 0 0 / 68.571%),
      0 0 5px oklch(100% 0 0 / 0%),
      0 0 25px oklch(100% 0 0 / 57%);
  }
  44% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.314em 0.257em 28.143px oklch(100% 0 0 / 11.429%),
      -0.643em 0.943em 16.571px oklch(100% 0 0 / 45.714%),
      0 0 5px oklch(100% 0 0 / 80%),
      0 0 20px oklch(100% 0 0 / 42%);
  }
  45% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -0.857em 1.257em 20.429px oklch(100% 0 0 / 34.286%),
      0.286em -0.243em 8.857px oklch(100% 0 0 / 68.571%),
      0 0 18px oklch(100% 0 0 / 35%);
  }
  48% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -1.5em 2.2em 32px oklch(100% 0 0 / 0%),
      1.143em -0.971em 20.429px oklch(100% 0 0 / 34.286%),
      0 0 14px oklch(100% 0 0 / 18%);
  }
  51% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -1.5em 2.2em 32px oklch(100% 0 0 / 0%),
      2em -1.7em 32px oklch(100% 0 0 / 0%),
      0 0 11px oklch(100% 0 0 / 6%);
  }
  60% {
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -1.5em 2.2em 32px oklch(100% 0 0 / 0%),
      2em -1.7em 32px oklch(100% 0 0 / 0%),
      0 0 10px oklch(100% 0 0 / 0%);
  }
  80% {
    opacity: .65;
    transform: scale(1.2333);
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -1.5em 2.2em 32px oklch(100% 0 0 / 0%),
      2em -1.7em 32px oklch(100% 0 0 / 0%),
      0 0 10px oklch(100% 0 0 / 0%);
  }
  100% {
    opacity: .45;
    transform: scale(1);
    text-shadow:
      -2em -1.4em 32px oklch(100% 0 0 / 0%),
      2.6em 0.1em 32px oklch(100% 0 0 / 0%),
      1.8em 1.8em 32px oklch(100% 0 0 / 0%),
      1.2em -2.3em 32px oklch(100% 0 0 / 0%),
      -2.7em 0.3em 32px oklch(100% 0 0 / 0%),
      -1.5em 2.2em 32px oklch(100% 0 0 / 0%),
      2em -1.7em 32px oklch(100% 0 0 / 0%),
      0 0 10px oklch(100% 0 0 / 0%);
  }
}

/* ### Section 30 - Layout & Navigation Transitions */

#contentContainer {
  transition-duration: 0.35s !important;
  /* transition-property: visibility, width, transform; */
  /* disabling for potential performance issues */
  transition-property: visibility, transform;
  transition-timing-function: var(--transition-curve);
}

ytmusic-app-layout.content-scrolled #mini-guide-background.ytmusic-app-layout,
ytmusic-app-layout[player-page-open] #mini-guide-background.ytmusic-app-layout {
    opacity: 0;
}

tp-yt-paper-item.ytmusic-guide-entry-renderer {
  opacity:0.7;
  transition: none;
    
    &:hover{
    background-color: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
    opacity:1;
    }
}

ytmusic-guide-entry-renderer[active] tp-yt-paper-item.ytmusic-guide-entry-renderer {
  background-color: var(--white-dot-10);
  /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
  opacity: 0.87;
  transition: none;

  &:hover {
    opacity: 1;
    background-color: var(--white-dot-15);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }
}

/* ### Section 31 - Translation etc entry ######### */ 


/* ### Section 32 - Progress bar and knob ######### */
#hover-time-info.ytmusic-player-bar {
  background-color: var(--white-dot-87);
  color: var(--black-dot-87);
  font-size: 13px;
  font-weight: 600;
  padding: 5px 12px;
  border-radius: 20.88px;
  corner-shape: superellipse(1.2);
  top: -42px;
  transform: translateX(-50%);
  box-shadow: 0 4px 12px oklch(0 0 0 / 0.2);
}

tp-yt-paper-slider {
  --paper-progress-active-color: var(--white-dot-60);
  --paper-progress-secondary-color: var(--white-dot-10);
}

#progress-bar.ytmusic-player-bar[focused],
#progress-bar.ytmusic-player-bar,
ytmusic-player-bar[player-fullscreened] #progress-bar.ytmusic-player-bar,
ytmusic-player-bar:hover #progress-bar.ytmusic-player-bar,
#progress-bar.ytmusic-player-controls {
  --paper-slider-knob-color: var(--white-dot-60);
  --paper-slider-knob-start-color: var(--white-dot-60);
  --paper-slider-knob-start-border-color: var(--white-dot-60);
  --paper-slider-container-color: var(--white-dot-10);
  --paper-slider-active-color: var(--paper-progress-active-color);
  --paper-slider-secondary-color: var(--paper-progress-secondary-color);
}

#progress-bar.ytmusic-player-bar {
  --paper-slider-knob-color: transparent;
  --paper-slider-knob-start-color: transparent;
  --paper-slider-knob-start-border-color: transparent;
}

ytmusic-player-bar[is-mweb-player-bar-modernization-enabled]
  #progress-bar.ytmusic-player-bar,
ytmusic-player-bar[is-mweb-player-bar-modernization-enabled]
  #progress-bar.ytmusic-player-bar[focused] {
  --paper-slider-disabled-active-color: var(--white-dot-87);
  --paper-slider-disabled-secondary-color: var(--white-dot-10);
  --paper-slider-disabled-knob-color: transparent;
  --paper-slider-container-color: var(--white-dot-10);
  --paper-slider-knob-color: transparent;
  --paper-slider-knob-start-color: transparent;
  --paper-slider-knob-start-border-color: transparent;
}

.slider-knob-inner.tp-yt-paper-slider {
  width: calc(100% - 10px) !important;
  height: calc(100% - 20px) !important;
  border-radius: 999rem !important;
  corner-shape: superellipse(1.2) !important;
  position: absolute !important;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  margin: auto !important;
}

 /* disabling for potential performance issues */

.time-info.ytmusic-player-bar,
.current-time.ytmusic-player-controls,
.total-time.ytmusic-player-controls {
  color: var(--white-dot-87) !important;
  font-weight: 200 !important;
}

/* ### Section 33 - Audio-Video toggle ############ */
#av-id {
  .av-toggle.ytmusic-av-toggle {
    background-color: var(--white-dot-10);
    border: none;
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
    display: inline-flex;
    overflow: hidden;
    corner-shape: superellipse(1.2);

    button {
      corner-shape: superellipse(1.2);
      border: none;
    }
    
    ytmusic-app-layout[player-ui-state="FULLSCREEN"] & {
      background-color: var(--black-dot-60);
      /*box-shadow: inset 0 0 0px 1px var(--black-dot-60);*/
      outline: 1px solid var(--black-dot-60);
      outline-offset: -1px;
      border: none;
    }
  }

  ytmusic-av-toggle {
    opacity: 0.6;
    transition: opacity 0.3s var(--transition-curve);

    &:hover {
      opacity: 0.87;
    }

    :is(.song-button, .video-button)[aria-pressed="true"] {
      background-color: var(--white-dot-15);
      color: var(--white-1);
      /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
      outline: 1px solid var(--white-dot-10);
      outline-offset: -1px;
    }

    &[toggle-disabled] {
      opacity: 0.1;

      &:hover {
        opacity: 0.4;
      }

      button.ytmusic-av-toggle {
        cursor: default;

        &[aria-pressed="false"] {
          color: var(--white-dot-15);
        }
      }
    }
  }

  :is(.song-button, .video-button).ytmusic-av-toggle {
    background-color: transparent;
    color: var(--white-dot-60);
    border: none;
    margin: 0;
    cursor: pointer;
    transition: background-color 0.2s var(--transition-curve),
      color 0.2s var(--transition-curve);
  }
}

@media (min-width: 1364px) and (max-width: 1577px) {
  ytmusic-player-page {
    --ytmusic-player-page-vertical-padding: calc(40px * 0.4);
  }
}

/* ### Section 34 - Play-next buttons ############# */
.play-pause-button-wrapper.ytmusic-player-controls {
  background-color: var(--white-dot-10);
  /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
  opacity: 0.87;
  transition: none;

  &:hover {
    opacity: 1;
    background-color: var(--white-dot-15);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }
}

.yt-icon:where(.yt-icon-container),
.yt-icon:where(.ytIconWrapperHost),
yt-icon {
  color: var(--white-1);
  
  &:hover {
    opacity: 1;
  }
}

#button:not(ytmusic-responsive-header-renderer #button) > yt-icon > span {
  opacity: 0.87;

  &:hover {
    opacity: 1;
  }
}

ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .next-button.ytmusic-player-controls,
ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .previous-button.ytmusic-player-controls,
ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .seek-back-button.ytmusic-player-controls,
ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .seek-forward-button.ytmusic-player-controls,
ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .repeat.ytmusic-player-controls,
ytmusic-player-controls[is-web-delhi-icons-enabled]:not([player-fullscreened])
  .shuffle.ytmusic-player-controls,
ytmusic-player-bar[is-web-delhi-icons-enabled]
  yt-icon-button.ytmusic-player-bar,
.middle-controls-buttons .ytSpecButtonShapeNextMono.ytSpecButtonShapeNextText,
#top-level-buttons .ytSpecButtonShapeNextMono.ytSpecButtonShapeNextText,
ytmusic-menu-renderer:not(ytmusic-responsive-header-renderer ytmusic-menu-renderer) #button-shape > button {
  opacity: 0.7;
  transition: none;
  border-radius: 50%;

  &:hover {
    background-color: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
    outline: 1px solid var(--white-dot-10);
    outline-offset: -1px;
    border-radius: 50%;
    opacity: 1;
  }
}

/* ## SECTION-35 - Album & Playlist Header Buttons# */
ytmusic-responsive-header-renderer[is-album-detail-page][num-header-buttons="3"]
  .action-buttons.ytmusic-responsive-header-renderer
  .ytmusic-responsive-header-renderer:not(:nth-child(2)),
ytmusic-responsive-header-renderer[is-album-detail-page][num-header-buttons="3"]
  .action-buttons.ytmusic-responsive-header-renderer
  yt-button-renderer.ytmusic-responsive-header-renderer:nth-child(2),
ytmusic-responsive-header-renderer[is-album-detail-page][num-header-buttons="4"]
  .action-buttons.ytmusic-responsive-header-renderer
  > .ytmusic-responsive-header-renderer,
ytmusic-responsive-header-renderer[is-album-detail-page][num-header-buttons="5"]
  .action-buttons.ytmusic-responsive-header-renderer
  .ytmusic-responsive-header-renderer:not(:nth-child(3)),
ytmusic-responsive-header-renderer[is-playlist-detail-page][num-header-buttons="3"]
  .action-buttons.ytmusic-responsive-header-renderer
  .ytmusic-responsive-header-renderer:not(:nth-child(2)),
ytmusic-responsive-header-renderer[is-playlist-detail-page][num-header-buttons="3"]
  .action-buttons.ytmusic-responsive-header-renderer
  yt-button-renderer.ytmusic-responsive-header-renderer:nth-child(2),
ytmusic-responsive-header-renderer[is-playlist-detail-page][num-header-buttons="4"]
  .action-buttons.ytmusic-responsive-header-renderer
  > .ytmusic-responsive-header-renderer,
ytmusic-responsive-header-renderer[is-playlist-detail-page][num-header-buttons="5"]
  .action-buttons.ytmusic-responsive-header-renderer
  .ytmusic-responsive-header-renderer:not(:nth-child(3)) {
  background: transparent;
  /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
  outline: 1px solid var(--white-dot-15);
  outline-offset: -1px;

  &:hover {
    background: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }
}

ytmusic-responsive-header-renderer
  #action-buttons
  > ytmusic-play-button-renderer {
  background-color: var(--white-dot-10);
  /*box-shadow: inset 0 0 0px 1px var(--white-dot-10);*/
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
  opacity: 0.87;
  &:hover {
    background-color: var(--white-dot-15);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
    opacity: 1;
  }
}

ytmusic-responsive-header-renderer
  #action-buttons
  > :not(ytmusic-play-button-renderer) {
  opacity: 0.87;

  &:hover {
    opacity: 1;
  }

  & yt-icon > span {
    opacity: inherit !important;
  }
}

ytmusic-responsive-header-renderer
  .ytSpecButtonShapeNextMono.ytSpecButtonShapeNextText:hover {
  background: transparent;
  background-color: transparent;
}

ytmusic-responsive-header-renderer .icon.ytmusic-play-button-renderer {
  color: inherit;
}

/* ## SECTION-36 - Track Lists #################### */

#contents > ytmusic-responsive-list-item-renderer {
  box-shadow: none;
  border-radius: 13.92px;
  corner-shape: superellipse(2);
  border: none !important;

  &:hover {
    background: var(--white-dot-10);
    /*box-shadow: inset 0 0 0px 1px var(--white-dot-15);*/
    outline: 1px solid var(--white-dot-15);
    outline-offset: -1px;
  }

  & .secondary-flex-columns.ytmusic-responsive-list-item-renderer .flex-column.ytmusic-responsive-list-item-renderer {
    font-weight: 200;
  }
}

/* ## SECTION-37 - Blyrics Dock ################### */
.blyrics-dock {
  transition: transform 125ms cubic-bezier(0.4, 0, 0.2, 1);
}

.blyrics-dock__inner {
  background-color: var(--white-dot-10);
  background: oklch(from var(--paper-listbox-background-color, var(--primary-background-color)) l c h / 0.75);
  box-shadow: none;
  outline: 1px solid var(--white-dot-15);
  outline-offset: -1px;
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
  transition:
    transform 320ms cubic-bezier(0.4, 0, 0.2, 1),
    opacity 320ms cubic-bezier(0.4, 0, 0.2, 1),
    background-color 200ms ease;
}

.blyrics-dock__inner:hover,
.blyrics-dock__inner:focus-within,
.blyrics-dock__inner--expanded {
  background-color: var(--black-dot-87);
  background: var(--paper-listbox-background-color, var(--primary-background-color));
}

.blyrics-dock--hidden .blyrics-dock__inner,
#layout[player-fullscreened]:not([blyrics-dfs]) .blyrics-dock--idle-hidden .blyrics-dock__inner {
  filter: none;
}

.blyrics-dock__divider {
  background-color: var(--white-dot-10);
}

.blyrics-dock__control,
.blyrics-dock__cycle,
.blyrics-dock__offset-step,
.blyrics-dock__offset-row-step {
  color: var(--white-dot-87);
  transition:
    background-color 160ms cubic-bezier(0.4, 0, 0.2, 1),
    color 160ms cubic-bezier(0.4, 0, 0.2, 1);
}

.blyrics-dock__control:hover,
.blyrics-dock__cycle:hover,
.blyrics-dock__offset-step:hover,
.blyrics-dock__offset-row-step:hover {
  background-color: var(--white-dot-15);
  color: var(--white-1);
}

.blyrics-dock__control--active {
  box-shadow: none;
  outline: 1px solid color-mix(in srgb, var(--dock-pill-accent, hsl(45, 100%, 56%)) 30%, transparent);
  outline-offset: -1px;
}

.blyrics-dock__control--active:hover {
  box-shadow: none;
  outline: 1px solid color-mix(in srgb, var(--dock-pill-accent, hsl(45, 100%, 56%)) 45%, transparent);
  outline-offset: -1px;
}

.blyrics-dock__cycle,
.blyrics-dock__offset-step {
  transition:
    background-color 160ms cubic-bezier(0.4, 0, 0.2, 1),
    color 160ms cubic-bezier(0.4, 0, 0.2, 1),
    max-width 240ms cubic-bezier(0.4, 0, 0.2, 1),
    opacity 240ms cubic-bezier(0.4, 0, 0.2, 1);
}

.blyrics-dock__fx {
  transition:
    transform 320ms cubic-bezier(0.4, 0, 0.2, 1),
    opacity 320ms cubic-bezier(0.4, 0, 0.2, 1),
    width 320ms cubic-bezier(0.4, 0, 0.2, 1);
}

.blyrics-dock__fx-out {
  filter: none;
}

.blyrics-dock__source {
  color: var(--white-dot-87);
}

.blyrics-dock__source-position {
  color: var(--white-dot-87);
}

.blyrics-dock__menu {
  background-color: var(--black-dot-87);
  background: var(--paper-listbox-background-color, var(--primary-background-color));
  box-shadow: none;
  outline: 1px solid var(--white-dot-15);
  outline-offset: -1px;
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
}

.blyrics-dock__menu-item {
  color: var(--white-dot-87);
}

.blyrics-dock__menu-item:hover {
  background-color: var(--white-dot-10);
  color: var(--white-1);
}

/* --- Offset control --- */
.blyrics-dock__offset {
  color: var(--white-dot-87);
}

.blyrics-dock__offset-icon:hover {
  background-color: var(--white-dot-15);
}

.blyrics-dock__offset-value {
  color: var(--white-dot-87);
}

.blyrics-dock__offset-row-label {
  color: var(--white-dot-87);
}

.blyrics-dock__offset-reset {
  background-color: var(--white-dot-10);
  color: var(--white-dot-87);
}

.blyrics-dock__offset-reset:hover {
  background-color: var(--white-dot-15);
  color: var(--white-1);
}

@keyframes blyrics-dock-offset-flash-up {
  0% {
    color: var(--dock-accent, hsl(45, 95%, 65%));
    transform: scale(1.22);
  }
  100% {
    color: var(--white-dot-87);
    transform: scale(1);
  }
}

@keyframes blyrics-dock-offset-flash-down {
  0% {
    color: var(--dock-accent, hsl(45, 95%, 65%));
    transform: scale(0.8);
  }
  100% {
    color: var(--white-dot-87);
    transform: scale(1);
  }
}

/* ## SECTION-38 - Autoscroll Resume button ####### */
.autoscroll-resume-button {
  background: var(--paper-listbox-background-color, var(--primary-background-color));
  opacity: 0.75;
  border: none;
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
  color: var(--white-dot-87);
  font-weight: 500;
  backdrop-filter: none;
  will-change: auto;

  transition:
    opacity 0.15s linear 0.05s,
    transform 0.2s cubic-bezier(0.5, 1, 0.89, 1);
}

.autoscroll-resume-button:hover {
  text-shadow: none;
  opacity: 1;
  color: var(--white-1);
}

.autoscroll-resume-button:focus {
  outline-color: var(--white-dot-10);
  outline: 1px solid var(--white-dot-10);
  outline-offset: -1px;
}

/* ################################################ */


} @else {

#sliderKnob {display: none;}

}
